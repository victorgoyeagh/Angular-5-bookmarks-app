export const environment = {
    production: true,
    config: {
        dictionary: {
            appTitle: "Bookmarks"
        },
        storageType: 'localStorage',
        cookieName: 'bookmarks',
        pagination: {
            defaultItemsPerPage: 10
        }
    }
};
